/// AI Agent 共享常量。
class AiAgentConstants {
  AiAgentConstants._();

  /// 默认最大迭代次数。
  static const int defaultMaxIterations = 30;

  /// 默认最大工具调用次数。
  static const int defaultMaxToolCalls = 20;

  /// 默认最大 SQL 执行次数。
  static const int defaultMaxSqlExecutions = 100;
}
